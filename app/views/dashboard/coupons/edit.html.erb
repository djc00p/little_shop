<%= form_for @coupon, url: dashboard_coupon_path(@coupon) do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :dollars_off %>
    <%= f.text_field :dollars_off %>
  </p>

  <%= f.submit %>
<% end %>
